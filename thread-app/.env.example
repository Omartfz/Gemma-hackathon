# Temporary cloud extract (Phase 2) — OpenAI stand-in for Gemma
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# cloud = OpenAI for now; set to gemma when Google AI is wired
EXTRACT_CLOUD_PROVIDER=openai

# Local Gemma via Ollama (optional)
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=gemma4:e2b

# Future Google AI / Gemma cloud (not used until EXTRACT_CLOUD_PROVIDER=gemma)
GOOGLE_API_KEY=
# GEMINI_API_KEY=
GEMMA_CLOUD_MODEL=
